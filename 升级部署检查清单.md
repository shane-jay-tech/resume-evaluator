# 升级部署检查清单

## P0 数据层与配置改造

- [x] `.env` — 密钥外置（DEEPSEEK_API_KEY, AUTH_TOKEN）
- [x] `config.yaml` — YAML 配置（6 岗位完整配置）
- [x] `utils/config.py` — YAML + .env 配置加载
- [x] `utils/logger.py` — TimedRotatingFileHandler 日志
- [x] `database.py` — SQLite DataStore（results/pending/processed/history 表）
- [x] `main.py` — DataStore 集成 + auth 中间件 + 新端点
- [x] `parser.py` — OCR 预处理 + 作品集链接提取

## P1 前端增强

- [x] `dashboard.html` — htmx CDN + auth + undo + AI排序 + 标杆推荐 + 删除恢复
- [x] `pipeline.html` — htmx CDN + auth + 看板状态流转
- [x] `weekly_report.html` — htmx CDN + auth
- [x] PDF 导出 — 封面页 + 概要页 + 候选人明细

## P2 业务增强

- [x] AI 排序 `/api/rank` — LLM 综合排序
- [x] 标杆推荐 `/api/promote-to-reference` — 候选人推荐为标杆
- [x] 标杆审批 `/api/approve-reference` — 管理员审批
- [x] 作品集链接提取 — parser.extract_portfolio_links
- [x] 动态评估维度 — 从 config 读取每个岗位的维度定义

## P3 系统增强

- [x] 并发评估 `/api/evaluate/batch` — ThreadPoolExecutor
- [x] 健康检查 `/api/health` — 运行状态 + 统计
- [x] 删除恢复 `/api/undo` — 软删除恢复
- [x] 状态流转 `/api/transition` — Pipeline 状态管理
- [x] 待分配验证 `/api/verify` — 查看原始文本
- [x] `tests/test_parser.py` — parser 单元测试
- [x] `tests/test_evaluator.py` — evaluator 单元测试
- [x] `requirements.txt` — 完整依赖列表

## 新增 API 端点

| 端点 | 方法 | 用途 |
|------|------|------|
| `/api/health` | GET | 系统健康检查 + 统计 |
| `/api/deleted` | GET | 最近删除列表 |
| `/api/approvals` | GET | 待审批标杆列表 |
| `/api/undo` | POST | 恢复删除 |
| `/api/rank` | POST | AI 排序 |
| `/api/transition` | POST | Pipeline 状态流转 |
| `/api/promote-to-reference` | POST | 推荐标杆 |
| `/api/approve-reference` | POST | 审批标杆 |
| `/api/verify` | POST | 验证待分配项 |
| `/api/evaluate/batch` | POST | 并发批量评估 |
| `/api/queue` | POST | 待分配池排序 |

## 认证说明

- 所有 POST 端点需 Bearer token 认证
- Token 配置在 `.env` 的 `AUTH_TOKEN`
- 未配置 token 时跳过认证（向后兼容）
- 前端通过 `localStorage.auth_token` 传递

## 部署步骤

1. `pip3 install -r requirements.txt`
2. 检查 `.env` 配置（DEEPSEEK_API_KEY + AUTH_TOKEN）
3. 检查 `config.yaml` 配置（岗位、维度、权重等）
4. `python3 main.py config.yaml`
5. 首次启动自动从 JSON 迁移数据到 SQLite
6. 访问 http://127.0.0.1:18980/dashboard.html

## 回滚说明

- 旧 JSON 文件不会被删除，可用旧 main.py + config.json 回滚
- SQLite 数据库在 `data/recruitment.db`

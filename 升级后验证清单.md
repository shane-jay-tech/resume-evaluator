# 升级后验证清单 (P1+ → P3+)

## 启动验证

- [ ] 启动系统: `python3 main.py config.yaml`
- [ ] 启动日志显示 `[archive]` 归档信息（可能显示"无符合归档条件的记录"即正常）
- [ ] 访问 http://127.0.0.1:18980/dashboard.html 可打开
- [ ] 访问 http://127.0.0.1:18980/pipeline.html 可打开
- [ ] 访问 http://127.0.0.1:18980/weekly_report.html 可打开
- [ ] 访问 http://127.0.0.1:18980/quality.html 可打开

---

## P1+: SSE 实时事件推送

### 后端
- [ ] `GET /api/events?token=***REMOVED***` 返回 `text/event-stream`，定期 heartbeat
  ```bash
  curl -N "http://127.0.0.1:18980/api/events?token=***REMOVED***"
  ```

### 评估触发广播
- [ ] 将一份新简历放到 `~/Downloads`，系统评估完成后：
  - [ ] SSE 客户端收到 `event: eval_complete` 
  - [ ] 包含 `candidate_name`, `match_score`, `verdict`, `id` 字段

### 状态变更广播
- [ ] 在 Pipeline 页面将候选人从"待筛选"拖到"面试中"：
  - [ ] SSE 客户端收到 `event: status_change`
  - [ ] 包含 `from`, `to`, `candidate_name` 字段

### 待分配池广播
- [ ] 一份岗位不明确的简历被评估后：
  - [ ] SSE 客户端收到 `event: pending_update`
  - [ ] Dashboard 待分配池实时更新

### 前端降级
- [ ] 打开 dashboard.html，浏览器 DevTools Network 中可见 `/api/events` EventSource 连接
- [ ] 停止服务器：浏览器控制台不报未捕获错误，30s 后自动重连
- [ ] 重连前降级为 5s/8s 轮询

---

## P1+: 评估质量监控面板

### quality.html
- [ ] 页面显示趋势折线图 + 推荐率折线图 + 可靠性柱状图 + 终态对比图
- [ ] 切换「近 7 天 / 近 14 天 / 近 30 天」按钮，图表和数据表更新
- [ ] 数据表显示每日评估数、平均分、推荐率、趋势箭头

### Drift 警告
- [ ] `GET /api/health` 返回中包含 `drift_warning` 字段
- [ ] 当 7 天均分与 30 天均分偏差 >15% 时，quality.html 显示黄色警告 banner

### API 端点
- [ ] `GET /api/stats/eval-trends?days=7` 返回每日趋势数组
- [ ] `GET /api/stats/eval-reliability` 返回各岗位终态可靠性

---

## P3+: 数据归档

### 配置
- [ ] `config.yaml` 包含 `archive` 节（enabled, retention_days: 90, archive_dir）

### 归档执行
- [ ] 启动日志包含 `[archive]` 行（说明归档逻辑已执行）
- [ ] 若系统有超过 90 天的已通过/已淘汰记录，会被归档到 `archives/YYYY-Qn/archive_TIMESTAMP.json`
- [ ] 被归档的记录从 results 表中硬删除（`DELETE FROM results WHERE id IN (...)`)
- [ ] `GET /api/health` 返回 `last_archive_count` 字段

---

## P2+: 招聘漏斗 AI 归因分析

### 配置
- [ ] `config.yaml` 包含 `funnel.baselines` 节

### API
- [ ] `POST /api/insights/funnel-analysis`（需 Bearer token）返回：
  - [ ] `stages`: 各阶段人数
  - [ ] `conversions`: 转化率 vs 基线
  - [ ] `bottleneck_stage`: 瓶颈阶段名
  - [ ] `analysis`: AI 分析文字
  - [ ] `suggestions`: 改进建议列表

### weekly_report.html
- [ ] 页面加载后，在策略建议下方出现"🔍 漏斗瓶颈洞察"卡片
- [ ] 显示转化率数值和基线对比
- [ ] 瓶颈阶段有颜色标识（红=需关注，绿=优于基线）
- [ ] 有 AI 分析文字和改进建议列表

---

## P2+: 标杆简历标签化与相似度雷达图

### 数据库
- [ ] `reference_features` 表已创建（检查 `data/recruitment.db`）

### 特征提取
- [ ] 在 dashboard 中对候选人点击"🏷️特征"按钮
  - [ ] 按钮显示 loading 状态后恢复
  - [ ] POST /api/references/extract-features 调用成功
  - [ ] 结果中 feature 包含 tech_stack, years, industry, education, skills

### 雷达图
- [ ] 特征提取后，详情面板出现 radar 图（polar area chart）
- [ ] 显示标杆简历的技术栈、行业、能力、经验、学历维度
- [ ] `GET /api/references/features?position=XXX` 返回该岗位已提取的特征

---

## P3+: Prometheus 指标

### /metrics 端点
- [ ] `GET /metrics` 返回 Prometheus exposition format 文本（无需 auth）
  ```bash
  curl http://127.0.0.1:18980/metrics
  ```
- [ ] 输出包含：
  - [ ] `eval_requests_total{status="success"}` / `{status="error"}` / `{status="skipped"}`
  - [ ] `pipeline_status_changes_total{from_status,to_status}`
  - [ ] `queue_size{type="results"}` / `{type="pending"}`
  - [ ] `eval_duration_seconds_bucket` / `_sum` / `_count`

### 指标采集
- [ ] 评估一份简历后，`eval_requests_total` 相应 status 计数 +1
- [ ] Pipeline 状态变更后，`pipeline_status_changes_total` 计数 +1
- [ ] 访问 `/api/health` 后，`queue_size` 更新为当前队列数

---

## 向后兼容验证

- [ ] `GET /api/results` 返回结果格式不变
- [ ] `POST /api/save` 正常保存编辑
- [ ] `POST /api/delete` 软删除正常
- [ ] `POST /api/undo` 恢复删除正常
- [ ] `POST /api/assign` 手动分配正常
- [ ] `POST /api/rank` AI 排序正常
- [ ] `POST /api/transition` Pipeline 流转正常
- [ ] `GET /api/export/pdf?position=all` 导出正常
- [ ] 已有 11 个单元测试全部通过
  ```bash
  python3 tests/test_parser.py && python3 tests/test_evaluator.py
  ```

---

## 安全验证

- [ ] `/metrics` 端点无需 token 即可访问（公开指标）
- [ ] `/api/events?token=xxx` 通过 query string 验证 token
- [ ] 所有 POST 端点需要 Bearer token（`AUTH_TOKEN` 配置值）
